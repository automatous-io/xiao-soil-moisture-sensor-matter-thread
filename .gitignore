# Build output
build/
sdkconfig
sdkconfig.old
read-partition-table.bin
.cache/

# Release artifacts, built by scripts/make-release-bin.py and make-matter-ota.py
*.ota
automatous-io-*.bin

# Managed components — downloaded automatically, not committed.
# dependencies.lock IS committed so builds resolve identical versions.
managed_components/

# Custom-board exploration lives outside this firmware repo (64 MB file)
Kicad.kicad_pcb

# macOS
.DS_Store
**/.DS_Store

# VSCode
.vscode/
.history/

# Python cache
__pycache__/
*.pyc
*.pyo

# IDF component manager
idf_component_manager.yml
.idf_component_manager/
# VS Code devcontainer features lock (regenerated; no features declared)
.devcontainer/devcontainer-lock.json
